#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("enter the total amount of cash which pooja has");
    printf("the transaction she wishes to do");
    scanf("%d %d", &a,&b);
    if(b=5*b)
    a=a-0.50;
    else
    a=a; 
    printf("%d",a);
}


